name: Deploy to GitHub Pages

on:
  push:
    branches: [master]
    paths: [site/**]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Geteilt mit lagebild.yml und nyt.yml: nur EIN aktives github-pages-Deployment.
# cancel-in-progress: false, damit ein Menschen-Push hier keinen laufenden
# Editions-Deploy abbricht — alle drei Workflows serialisieren konsistent.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site
      - id: deployment
        uses: actions/deploy-pages@v4
